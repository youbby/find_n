# 삼각형 기하학적 유사성 기반 시계열 반복주기(N) 검출 기법
### — 넓이(S) 지표의 한계와 높이(H) 지표로의 전환

---

## 초록 (Abstract)

설비 알람/이벤트의 발생 시각(time)과 그 간격(tact)으로 구성된 시계열 데이터에서, tact 값이 주기 N을 갖고 반복되는지, 반복된다면 그 주기 N이 얼마인지를 검출하는 기하학적 방법을 제안한다. 초기 아이디어는 인덱스 n을 중심으로 N만큼 떨어진 세 점 A(n), B(n-N), C(n+N)이 이루는 삼각형의 **변 비율(R = |AB|/|AC|)**과 **넓이(S)**가 N이 실제 주기와 일치할 때 각각 1과 0에 가까워진다는 관찰에서 출발했다. 그러나 S를 그대로 지표로 사용할 경우 N 자체에 대한 스케일 종속성이 존재하여 서로 다른 N 후보 간 비교가 불가능하다는 문제가 확인되었고, 이를 해결하기 위해 밑변(BC)으로 정규화한 **높이(H = 2S/|BC|)**로 지표를 전환하였다. 이후 R(형태 조건)과 H(평탄도 조건)를 하나의 지표로 결합하려는 시도에서 **κ = 1 + cosθ**(θ는 A에서의 내각)라는 각도 기반 통합 지수를 검토하였으나, 같은 N에서의 주기/비주기 판별력은 H보다 월등히 우수한 반면 κ 자체가 1/N²로 스케일 종속성을 갖는다는 것이 이론적·실증적으로 확인되어 최종적으로는 **R(θ)과 H의 조합**을 채택하였다. 합성 데이터 및 실측 데이터를 통해 지표들의 거동을 비교 검증한 결과를 보고한다.

---

## 1. 서론

제조 설비의 알람/이벤트 로그는 다음과 같은 구조를 가진다.

| no | time | tact |
|---|---|---|
| 1 | time₁ | Null |
| 2 | time₂ | time₂ − time₁ |
| ... | ... | ... |
| n | timeₙ | timeₙ − timeₙ₋₁ |

여기서 tact(간격 시간)가 특정 주기 N을 두고 반복되는 패턴을 보이는 경우가 실무에서 관찰된다. 예를 들어 N=4인 경우 `10, 0.1, 5, 0.1, 10, 0.1, 5, 0.1 ...` 과 같은 형태다. 본 논문의 목표는 이러한 **미지의 반복 주기 N을 데이터로부터 검출**하는 것이다.

---

## 2. 문제 정의

- 입력: 시계열 (xₙ, yₙ), 단 xₙ = timeₙ (누적 시각), yₙ = tactₙ = xₙ − xₙ₋₁
- 목표: yₙ이 주기 N으로 반복되는 패턴을 가질 때, 그 N을 탐지
- 제약: N을 사전에 알 수 없으므로 N = 1, 2, 3, ... 후보를 순차 탐색해야 함
- 판별 기준 후보: N이 실제 주기(혹은 그 배수)와 일치할수록 "패턴이 반복된다"는 기하학적 증거가 강해야 함

---

## 3. 초기 접근법: 변 비율(R)과 넓이(S)

### 3.1 아이디어

인덱스 n을 기준으로 다음 세 점을 정의한다.

- A = (xₙ, yₙ)
- B = (xₙ₋ₙ, yₙ₋ₙ)
- C = (xₙ₊ₙ, yₙ₊ₙ)

만약 N이 실제 반복 주기와 일치하면, yₙ₋ₙ ≈ yₙ ≈ yₙ₊ₙ (같은 위상에서 값이 반복)이므로 세 점 A, B, C는 **거의 일직선 상에 놓인, 매우 납작한 삼각형**을 이룬다. 이때:

- **R = |AB| / |AC|** → 1에 수렴 (A가 B, C로부터 대칭적 위치)
- **S (삼각형 면적)** → 0에 수렴 (세 점이 일직선에 가까움)

반대로 N이 주기와 무관하면 세 점은 뾰족한 삼각형을 이루어 R은 1에서 벗어나고 S는 커진다.

### 3.2 수식

$$|AB| = \sqrt{(x_{n-N}-x_n)^2+(y_{n-N}-y_n)^2}, \quad |AC| = \sqrt{(x_{n+N}-x_n)^2+(y_{n+N}-y_n)^2}$$

$$S = \frac{1}{2}\left|(x_{n-N}-x_n)(y_{n+N}-y_n)-(y_{n-N}-y_n)(x_{n+N}-x_n)\right|$$

### 3.3 R의 각도(θ) 재정의

R(변 비율)은 dx, dy가 섞여 들어가 "납작함"을 직접적으로 반영하지 못하는 문제가 있어, A에서의 내각 θ로 재정의하였다.

$$\cos\theta = \frac{\vec{AB}\cdot\vec{AC}}{|AB||AC|}$$

N이 주기와 일치할수록 θ → π(180°)에 수렴한다 (A, B, C가 일직선).

---

## 4. 문제점: 넓이(S) 지표의 스케일 종속성

### 4.1 이론적 분석

N이 커지면 |AB|, |AC|(변의 길이)가 함께 커지므로, 삼각형이 "납작"하더라도 S의 절대 크기 자체가 N에 종속적으로 증가할 수 있다. N이 실제 주기의 정배수(N, 2N, 3N, ...)일 때, y값 차이(dy)는 주기 성분이 상쇄되고 노이즈 성분만 남아 N과 무관한 크기(O(1))를 유지하는 반면, x값 차이(dx)는 N에 비례해 커진다(~N·d, d는 평균 인덱스 간 시간 간격). 이를 넓이 식에 대입하면:

$$S \approx \frac{1}{2}Nd\,|{\Delta y_B + \Delta y_C}|$$

즉 **S는 N에 대해 선형으로 증가**한다. 서로 다른 N 후보의 S를 그대로 비교하면, "주기와 무관하게 N이 작을수록 S가 작아 보이는" 왜곡이 발생하여 올바른 비교가 불가능하다.

### 4.2 실증

동일한 합성 데이터(N=4 반복 패턴, 10만 샘플, 노이즈 포함)에 대해 10회 독립 시행(시드 고정 없음) 후 N=4, 8, 12, 16에서의 평균 S, 밑변 |BC|, 높이 H를 측정하였다.

| N | mean S | S/N | mean \|BC\| | \|BC\|/N | mean H |
|---|---|---|---|---|---|
| 4 | 7.530 | 1.883 | 30.419 | 7.605 | 0.49486 |
| 8 | 15.050 | 1.881 | 60.809 | 7.601 | 0.49494 |
| 12 | 22.587 | 1.882 | 91.237 | 7.603 | 0.49509 |
| 16 | 30.120 | 1.882 | 121.621 | 7.601 | 0.49531 |

S/N, |BC|/N이 N에 관계없이 상수로 유지되는 것이 확인되어, 앞선 이론적 분석(S, |BC| 모두 N에 선형 비례)이 실증적으로 뒷받침되었다.

### 4.3 결론

S를 원시(raw) 값 그대로 N 후보 간 비교 지표로 사용할 수 없다. **정규화가 필요**하다.

---

## 5. 제안 기법: 높이(H) 기반 주기 검출

### 5.1 정의

넓이 S를 밑변 |BC|로 정규화한 **삼각형의 높이(H)**를 지표로 채택한다.

$$H = \frac{2S}{|BC|} = \frac{\left|(x_{n-N}-x_n)(y_{n+N}-y_n)-(y_{n-N}-y_n)(x_{n+N}-x_n)\right|}{\sqrt{(x_{n+N}-x_{n-N})^2+(y_{n+N}-y_{n-N})^2}}$$

### 5.2 이론적 근거: H의 N-불변성

N이 실제 주기의 정배수일 때, S ~ Nd·|Δy|, |BC| ~ 2Nd이므로:

$$H = \frac{2S}{|BC|} \approx \frac{2\cdot Nd\,|\Delta y_B+\Delta y_C|/2}{2Nd} = \frac{|\Delta y_B+\Delta y_C|}{2}$$

**N이 완전히 약분되어 사라진다.** 즉 H는 정배수 조건 하에서 N에 무관하게 노이즈 크기 수준으로 수렴하는 반면, N이 주기와 무관한 경우에는 dy에 주기 성분이 그대로 남아 H가 유의미하게 커진다. 이 성질 덕분에 서로 다른 N 후보 간 H 값을 직접 비교하는 것이 타당해진다.

### 5.3 결합 판별 기준: 변 비율(R, 또는 θ) + 높이(H)

단일 지표만으로는 우연에 의한 오탐 가능성이 있으므로, 다음 두 조건을 함께 사용한다.

1. **형태 조건 (Shape condition)**: R = |AB|/|AC| ≈ 1 (또는 θ ≈ π) → 삼각형이 이등변에 가까움 (A가 B, C로부터 대칭적 위치에 있음을 보장 → 위상 정렬 확인)
2. **평탄도 조건 (Flatness condition)**: H ≈ 0 (임계값 이하) → 세 점이 실질적으로 일직선

두 조건을 모두 만족하는 N 후보를 "주기 정합(candidate period)"으로 판정한다.

### 5.4 알고리즘

```
입력: tact 배열 y[0..L-1], time 배열 x[0..L-1], 최대 탐색 주기 N_max
출력: 추정 주기 N*

1. for N in 1..N_max:
     H(N) = mean(H_n for n in [N, L-N))   # H_n: 인덱스 n에서의 높이
     R(N) = mean(R_n for n in [N, L-N))   # R_n: 인덱스 n에서의 변 비율(또는 |1-R_n| 평균)
2. 비주기 구간 대비 H(N)이 임계값 이하로 유의미하게 낮아지고, R(N)이 1에 근접하는
   가장 작은 N을 N*로 선택 (전역 최솟값이 아닌, 오름차순 스캔 중 첫 정합점을 채택)
3. N*를 반환
```

**주의**: 전역 최솟값(global argmin)으로 탐색할 경우, N*의 정배수(2N*, 3N*, ...)도 통계적으로 유사한 H값을 보이므로 노이즈에 의해 배수가 최솟값으로 오탐될 수 있다. 따라서 반드시 **작은 N부터 순차 탐색하여 첫 정합점에서 종료**하는 전략을 사용한다.

### 5.5 결합 지수의 모색: κ = 1 + cosθ

5.3절의 판별 기준은 R(또는 θ)과 H라는 **두 개의 별도 조건**을 동시에 검사해야 한다. 하나의 스칼라 지표로 두 조건(이등변 형태 + 평탄도)을 동시에 포착할 수 있다면 탐색 로직이 단순해진다는 동기에서, θ 하나로부터 두 조건을 모두 반영하는 지표를 모색하였다.

N이 실제 주기와 일치하면 θ → π이므로 cosθ → −1이다. 이때 자연스러운 후보는 다음과 같이 정의된다.

$$\kappa = 1 + \cos\theta$$

κ는 θ = π(원하는 완전 정합 케이스)일 때 0, θ = 0(퇴화된 반대 방향 케이스)일 때 2로 수렴한다. 7절에서 향후 과제로 언급되던 sinθ = 2S/(|AB||AC|)와 비교하면, sin(π−x) = sin(x)이므로 sinθ는 θ ≈ 0과 θ ≈ π를 구분하지 못하는 반면, **κ(즉 cosθ)는 두 경우를 부호로 구분**한다는 이론적 장점이 있어 R과 H를 대체할 유력한 단일 지표 후보로 검토되었다.

---

## 6. 실험 및 검증

### 6.1 실측 데이터 사례: H의 잔여 스케일 종속성

실제 알람 tact 데이터(드리프트 및 미세 노이즈 포함)에서 동일 앵커점 기준 N=4, N=8을 비교한 결과:

| N | \|AB\| | \|AC\| | \|BC\| | S | H | sinθ |
|---|---|---|---|---|---|---|
| 4 | 13.295 | 15.912 | 29.207 | 0.1579 | 0.0108 | 0.0015 |
| 8 | 27.848 | 31.151 | 58.999 | 0.0611 | 0.0021 | 0.0001 |

이 사례에서는 H가 오히려 감소했으나, 별도 사례(다른 앵커점)에서는 N이 2배가 될 때 H가 약 6.1배 증가하는 경우도 관찰되었다(0.222 → 1.366). 이는 실측 데이터에 존재하는 국소적 드리프트(추세)가 "정배수 조건에서 dy가 상쇄된다"는 이론적 가정을 깨뜨리기 때문으로 분석된다. 즉 **H는 순수 정상적(stationary) 주기 신호에서는 N-불변이지만, 실측 데이터의 비정상성(non-stationarity)에 대해서는 완전히 자유롭지 않다.**

### 6.2 합성 데이터: 주기 배수 vs 비주기 구간 비교

N=4 반복 패턴(평균 10 / 0.1 / 5 / 0.1, 표준편차 1.3333 / 0.0133 / 0.6667 / 0.0133)으로 10만 개 샘플을 생성하여 N=2, 4, 6, 8, 10, 12에서 H를 계산하였다.

| N | mean H | median H | std | max |
|---|---|---|---|---|
| 2 | 2.4942 | 0.0726 | 2.6511 | 10.5339 |
| **4** | **0.4978** | **0.0362** | 0.7699 | 6.9154 |
| 6 | 2.5072 | 0.0662 | 2.6569 | 10.4478 |
| **8** | **0.4955** | **0.0360** | 0.7698 | 7.2922 |
| 10 | 2.5090 | 0.0711 | 2.6588 | 11.0101 |
| **12** | **0.4996** | **0.0358** | 0.7722 | 6.6179 |

주기의 정배수(N=4, 8, 12)에서는 mean H가 약 0.50 수준으로 일관되게 낮았고, 비배수(N=2, 6, 10)에서는 약 2.5로 유의미하게 높았다. 이는 H가 **"주기 배수 vs 비배수"를 구분하는 지표로는 유효**함을 보여준다.

### 6.3 정배수 간 통계적 무차별성 검증 (10회 반복 시행)

N=4, 8, 12, 16(모두 참 주기의 정배수)에 대해 시드를 고정하지 않고 10회 독립 시행하여 평균 H의 분포를 비교하였다. 4.2절의 표에서 보인 바와 같이 S와 |BC|는 N에 선형 비례하여 증가했으나, H는 4자리 유효숫자 수준에서 N에 무관하게 일정하였다(0.4949 ~ 0.4953). 이는 5.2절의 이론적 유도와 정합한다.

**함의**: 참 주기의 정배수들끼리는 H 값으로 서로 구분되지 않는 것이 오히려 이론적으로 정상이며, 문제가 되는 것은 "정배수를 비주기로 오판하는 것"이 아니라 "정배수 중 가장 작은 값을 선택하는 탐색 전략"이다 (5.4절 참조).

### 6.4 결합 지수 κ = 1 + cosθ의 검증

5.5절에서 제안한 κ를 H와 동일한 두 실험(N-불변성, 정배수 vs 비배수 판별력)으로 검증하였다.

#### 6.4.1 N-불변성 검증

4.2절과 동일한 조건(N=4 반복 패턴, 10만 샘플, 시드 고정 없이 10회 독립 시행)에서 mean κ를 함께 측정하였다.

| N | mean H | mean κ |
|---|---|---|
| 4 | 0.49357 | 0.007180 |
| 8 | 0.49514 | 0.001818 |
| 12 | 0.49570 | 0.000807 |
| 16 | 0.49392 | 0.000450 |

mean H는 4.2절과 마찬가지로 N에 무관하게 상수(0.4936~0.4957)로 유지되었으나, **mean κ는 N이 커질수록 급격히 감소**하였다. N=4 값을 기준으로 (N/4)²로 나누면 N=8, 12, 16의 값과 정확히 일치하여(예: 0.007180/4=0.001795≈0.001818), κ가 **1/N²에 정확히 비례**함이 확인되었다.

#### 6.4.2 원인 분석: 정규화 차수의 불일치

κ가 왜 1/N²로 스케일 종속적인지 각 항의 스케일을 직접 분해하여 확인하였다(seed=42, N=4,8,12,16).

| N | S/N | \|AB\|/N | \|AC\|/N | BC/N | sinθ·N | κ·N² | mean H |
|---|---|---|---|---|---|---|---|
| 4 | 1.8753 | 3.8072 | 3.8073 | 7.6008 | 0.2579 | 0.1150 | 0.4931 |
| 8 | 1.8780 | 3.8004 | 3.8004 | 7.5974 | 0.2604 | 0.1159 | 0.4942 |
| 12 | 1.8771 | 3.7991 | 3.7991 | 7.5968 | 0.2605 | 0.1149 | 0.4941 |
| 16 | 1.8886 | 3.7987 | 3.7987 | 7.5965 | 0.2621 | 0.1161 | 0.4971 |

S, |AB|, |AC|, BC는 모두 **정확히 N¹에 비례**해서 커진다(4.1절 이론과 일치: x축 거리가 지배적이므로 변의 길이 자체가 N에 비례). H = 2S/BC는 분자·분모가 둘 다 N¹이므로 N이 정확히 약분되어 사라진다.

반면 κ(≈ sin²θ/2, θ≈π 근방 전개)는 sinθ = 2S/(|AB||AC|)를 한 번 더 제곱한 형태다. sinθ의 분모 |AB|·|AC|는 두 변의 곱이므로 **N²**로 커지는데, 분자 S는 N¹로만 커지므로 sinθ ~ 1/N (표의 sinθ·N이 상수인 것으로 확인)이고, κ ~ sin²θ ~ **1/N²**가 된다. 즉 κ는 "N¹ 넓이"를 "N² 크기의 정규화 분모"로 나눈 뒤 제곱까지 한 셈이어서, H가 확보했던 정규화 차수의 균형(분자 N¹ ÷ 분모 N¹)이 깨진 **과잉 정규화(over-normalization)**가 원인이다.

#### 6.4.3 판별력 비교

6.2절과 동일한 조건(seed=42, N=[2,4,6,8,10,12])에서 H와 κ의 정배수(4,8,12) vs 비배수(2,6,10) 판별력을 비교하였다.

| N | mean H | mean κ |
|---|---|---|
| 2 | 2.4946 | 0.329503 |
| 4 | 0.4931 | 0.007185 |
| 6 | 2.5075 | 0.049592 |
| 8 | 0.4942 | 0.001812 |
| 10 | 2.5093 | 0.018300 |
| 12 | 0.4941 | 0.000798 |

| 지표 | 정배수 mean | 비배수 mean | 판별비 (비배수/정배수) |
|---|---|---|---|
| H | 0.4938 | 2.5038 | 5.07배 |
| κ | 0.003265 | 0.132465 | **40.57배** |

같은 N 하나를 고정했을 때 κ의 주기/비주기 대비는 H보다 약 8배 더 뚜렷하다. 이는 κ ≈ H²/N² 관계로, H가 만드는 대비를 제곱해 증폭한 결과로 설명된다.

#### 6.4.4 결론: κ 기각, R+H 조합 유지

κ는 **고정된 N 하나에서의 이분법적 판별력**은 H보다 우수하지만, 6.4.1~6.4.2에서 확인된 1/N² 스케일 종속성 때문에 **서로 다른 N 후보를 비교해야 하는 5.4절 탐색 알고리즘에는 사용할 수 없다**. 이는 4절에서 S를 기각했던 것과 동일한 범주의 결함을 (반대 방향으로) 재도입한 것이다. 따라서 최종 지표 조합은 **R(또는 θ)과 H**로 유지한다. κ는 향후 "이미 후보 N이 정해진 상태에서의 최종 확인(confirmation)" 용도의 보조 지표로는 활용 가치가 있다.

### 6.5 요약

| 구분 | S (원시 넓이) | H (정규화 높이) | κ = 1+cosθ (결합 지수) |
|---|---|---|---|
| N에 대한 스케일 | 선형 증가 (~N) | 정배수 조건에서 불변 | 1/N²로 감소 |
| 주기 vs 비주기 구분력 (고정 N) | 있음 (그러나 N마다 절대 크기가 달라 비교 곤란) | 있음, 절대 크기 비교 가능 | 매우 강함 (H 대비 약 8배) |
| N 후보 간 비교 가능 여부 | 불가 | **가능** | 불가 |
| 실측(비정상) 데이터에서의 안정성 | 매우 취약 | 부분적 취약성 존재 (한계 참조) | 미검증 |
| 채택 여부 | 기각 | **채택** | 기각 (보조 지표로만 가치) |

---

## 7. 한계 및 향후 연구

1. **정상성 가정**: H의 N-불변성은 tact가 정상적(stationary) 확률 과정을 따를 때 이론적으로 보장된다. 실측 데이터의 추세/드리프트 하에서는 잔여 스케일 종속성이 존재함을 6.1절에서 확인하였다.
2. **완전 정규화 지표의 재검토 결과**: sinθ = 2S/(|AB|·|AC|) 및 이와 밀접한 κ = 1+cosθ는 두 변의 길이로 정규화되어 H보다 스케일 불변성이 더 강할 것으로 기대했으나, 6.4절 검증 결과 오히려 **1/N²의 새로운 스케일 종속성**을 갖는 것으로 확인되어 기각되었다. 완전한 스케일 불변성과 강한 판별력을 동시에 만족하는 단일 지표는 아직 발견하지 못했으며, 이는 향후 과제로 남는다.
3. **임계값 결정**: 5.4절 알고리즘에서 "유의미하게 낮은 H"를 판정하는 임계값을 데이터 기반으로 자동 산출하는 방법(예: 비주기 구간 H 분포의 하위 분위수 이용)이 정의되어 있지 않다.
4. **탐색 전략**: 전역 최솟값 대신 순차 스캔 후 첫 정합점을 택하는 전략의 오탐률을 다양한 노이즈 수준에서 정량 평가할 필요가 있다.
5. **이상치(outlier) 강건성**: 실측 데이터에는 설비 정지 등으로 인한 비정상적 스파이크가 섞일 수 있다. 동일 패턴·동일 기본 노이즈에 이상치 유무만 다르게 주입해 페어 비교한 결과, **median 기반 집계는 최대 10% 수준의 이상치 오염에도 N 판정이 전혀 흔들리지 않아(H, κ 모두 0건의 판정 변화) 강건함이 확인**되었다. 반면 mean 기반 집계는 5~10% 오염에서 판정 정확도가 크게 저하되었다(H 기준 최대 120/120 → 20/120). 이는 5.4절 알고리즘 및 실험 4 이후 지표 집계에 median을 사용한 설계 선택이 실무 데이터의 이상치에 대한 강건성 확보에 유효함을 뒷받침한다.

---

## 8. 결론

tact 반복 주기 검출을 위해 초기 제안된 넓이(S) 기반 지표는 N에 대한 선형 스케일 종속성으로 인해 서로 다른 N 후보 간 비교가 불가능하다는 한계가 실증적으로 확인되었다. 이를 밑변으로 정규화한 높이(H)로 전환한 결과, 참 주기의 정배수 조건에서 N-불변성을 갖는 것이 이론적 유도와 합성 데이터 실험 양쪽에서 검증되었다. 이어서 R(형태 조건)과 H(평탄도 조건)를 하나의 스칼라로 통합하려는 시도에서 κ = 1+cosθ를 제안·검증하였으나, 고정된 N에서의 판별력은 H보다 우수함에도 κ 자체가 1/N²의 스케일 종속성을 가져 N 후보 간 비교에는 부적합함이 확인되어 기각하였다. 이에 따라 **최종적으로 변 비율(R 혹은 θ)과 H를 결합한 판별 기준**, 그리고 작은 N부터 순차 탐색하는 알고리즘을 채택하였다. 다만 실측 데이터의 비정상성에 대한 H의 잔여 취약점은 여전히 남아 있으며, 완전한 스케일 불변성과 강한 판별력을 동시에 갖춘 지표의 탐색이 향후 과제로 남는다.

---

## Appendix A. 기호 정리

| 기호 | 의미 |
|---|---|
| xₙ | n번째 누적 시각 (time) |
| yₙ | n번째 간격 시간 (tact) |
| N | 검출하고자 하는 반복 주기 (후보) |
| A, B, C | 각각 (xₙ,yₙ), (xₙ₋ₙ,yₙ₋ₙ), (xₙ₊ₙ,yₙ₊ₙ) |
| R | 변 비율 \|AB\|/\|AC\| |
| θ | A에서의 내각 |
| S | 삼각형 ABC의 넓이 |
| H | 밑변 BC 기준 높이, H=2S/\|BC\| |
| sinθ | 2S/(\|AB\|\|AC\|), 완전 정규화 대안 지표 (6.4절에서 기각) |
| κ | 결합 지수, κ=1+cosθ (6.4절에서 기각, 보조 지표로만 가치) |

---

## Appendix B. 검증 코드 생성을 위한 Claude Code 프롬프트

> 아래 프롬프트를 그대로 Claude Code에 전달하여 본 논문의 실험(4.2, 6.1~6.4절)을 재현하는 검증 코드를 생성할 수 있다.

```
[목표]
"삼각형 기하학적 유사성 기반 시계열 반복주기(N) 검출 기법" 논문의 실험을
전부 재현하는 Python 검증 스크립트를 작성해줘.

[데이터 구조]
- x: 누적 시각(time), 1차원 numpy 배열
- y: 간격 시간(tact), y[n] = x[n] - x[n-1], 1차원 numpy 배열

[구현해야 할 함수]
1. generate_periodic_tact(L, pattern_params, seed=None)
   - pattern_params: [(mean, std), ...] 길이 P인 리스트 (P = 주기)
   - index i의 tact는 정규분포 N(pattern_params[i % P][0], pattern_params[i % P][1])에서 샘플링
   - 하한 1e-4로 클리핑 (간격은 음수 불가)
   - 반환: y (tact 배열), x = cumsum(y) (time 배열)

2. triangle_metrics(x, y, N) -> dict
   - 인덱스 n을 N..len(x)-N-1 범위에서 벡터화하여 다음을 모두 계산:
     A=(x[n],y[n]), B=(x[n-N],y[n-N]), C=(x[n+N],y[n+N])
     |AB|, |AC|, |BC| (유클리드 거리)
     R = |AB|/|AC|
     S = 0.5*|cross(AB, AC)|  (외적 기반 넓이)
     H = 2*S/|BC|
     theta = arccos(dot(AB,AC)/(|AB||AC|))
     sin_theta = 2*S/(|AB|*|AC|)
   - 각 항목을 numpy 배열로 반환 (길이 = len(x)-2N)

[실험 1: S의 N-선형성 검증]
- pattern_params = [(10.0,1.3333),(0.1,0.0133),(5.0,0.6667),(0.1,0.0133)] (주기 4)
- L=100,000, N_list=[4,8,12,16]
- 시드 고정 없이 10회 반복: 각 시행마다 데이터 재생성 후 N별 mean(S), mean(|BC|), mean(H) 계산
- 10회 평균을 표로 출력: N, mean S, S/N, mean|BC|, |BC|/N, mean H
- S/N과 |BC|/N이 N에 무관하게 상수(오차 1% 이내)로 유지되는지, mean H가 N에 무관하게
  상수(오차 1% 이내)로 유지되는지 assert로 검증

[실험 2: 주기 배수 vs 비주기 구간 비교]
- 동일 데이터(시드 고정, 예: seed=42)에서 N=[2,4,6,8,10,12]에 대해 H의
  mean/median/std/max 계산, 표로 출력
- 정배수(4,8,12)의 mean H가 비배수(2,6,10)의 mean H보다 유의미하게(예: 3배 이상)
  작은지 assert로 검증
- N별 H 분포를 하나의 Box plot으로 시각화 (matplotlib), PNG로 저장

[실험 3: 순차 탐색 알고리즘 구현 및 검증]
- find_period(x, y, N_max, n_trials=5) 함수 구현:
  N=1부터 N_max까지 순차적으로 mean H(N)을 계산하다가,
  H(N)이 이전 몇 개 N 대비 "국소 최솟값"이면서 전체 분포의 하위 20% 이내인
  첫 N을 반환 (임계값 산출 로직은 자유롭게 설계하되 근거를 주석으로 명시)
- 위 합성 데이터(N=4 반복 패턴)로 함수를 호출했을 때 반환값이 4인지 검증
- 임계값 설계가 어려우면, 대안으로 "N=1..12 범위에서 mean H가 국소 최솟값이며
  직전 대비 50% 이상 하락하는 첫 지점"을 사용해도 무방

[실험 4: 결합 지수 κ = 1+cosθ 검증 및 기각]
- triangle_metrics에 cos_theta = dot(AB,AC)/(|AB||AC|), kappa = 1+cos_theta 추가
- (실험 1과 동일 조건) N=[4,8,12,16], 시드 없이 10회 반복하여 mean κ도 함께 표에 출력
  - mean κ가 N에 무관한 상수인지(H와 동일 기준) 확인 → 성립하지 않음을 출력으로 보여줄 것
  - (mean κ) * N**2 을 함께 출력하여 이 값이 N에 무관한 상수로 수렴하는지 확인
- (실험 2와 동일 조건) N=[2,4,6,8,10,12]에서 H와 κ의 mean/median/std/max를 나란히 출력
  - 정배수 mean과 비배수 mean의 비율(판별비)을 H, κ 각각에 대해 계산 후 비교 출력
  - assert: κ의 판별비가 H의 판별비보다 큰지 (판별력 자체는 κ가 더 강함을 확인)
  - assert: (mean κ)*N**2 가 N=[4,8,12,16]에서 오차 5% 이내로 일정한지 (1/N² 스케일 종속성 확인 → 이 assert가 "통과"하는 것 자체가 κ의 한계를 실증하는 것)
- 콘솔에 "κ는 고정 N에서의 판별력은 우수하나 1/N² 스케일 종속성으로 인해 최종 채택은 R+H로 유지" 결론을 명시적으로 출력

[출력물]
- 콘솔에 모든 표를 정렬된 형태로 출력
- 실험 2, 실험 4의 Box plot을 PNG 파일로 저장 (H와 κ를 나란히 보여주는 서브플롯 권장)
- 각 실험의 assert 결과(성공/실패)를 명시적으로 출력

[주의사항]
- 모든 배열 연산은 for 루프 대신 numpy 벡터화로 작성
- 각 함수에 docstring으로 수식과 파라미터 설명 포함
- 실행 가능한 단일 .py 파일로 작성
```
